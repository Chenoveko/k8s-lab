apiVersion: v1
kind: PersistentVolumeClaim # Request for persistent storage that survives pod restarts
metadata:
  name: mealie-data
  namespace: mealie
spec:
  accessModes:
    - ReadWriteOnce # Can be mounted by a single node in read-write mode
  resources:
    requests:
      storage: 500Mi